{
  "version": "1.0.0",
  "skill": "autofix",
  "description": "Toggle autofix scanning: detect technical debt, open PRs for low-risk fixes, open issues for high-risk findings.",
  "input_schema": {
    "action": {
      "type": "string",
      "required": false,
      "source": "user prompt",
      "note": "One of: on, off, status, scan. Defaults to status."
    }
  },
  "output_schema": {
    "autofix_state": {
      "type": "object",
      "description": "Current autofix state: enabled flag, last scan time, finding counts",
      "required_fields": ["enabled"],
      "field_types": {
        "enabled": "bool"
      }
    }
  }
}
